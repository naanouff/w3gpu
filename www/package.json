{
  "name": "w3gpu-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build ../crates/w3gpu-wasm --target web --out-dir ../../www/pkg",
    "dev": "npm run build:wasm && vite",
    "vite": "vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
